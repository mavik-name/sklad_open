# Канон збірок MAVIK.NAME

## ПОТОЧНА КАНОНІЧНА БАЗА

**R188 — `188 Збірка_Сайт_MULTILANG_CORE.zip`**

SHA-256: `83988d928bffcb0d133b0ae712cd4dc0b2792222d5e2a1e29601546210ecd053`

Розмір: `54,496,973` байти.
Тип: `full-site-release`.
Managed files: `532`.
Статус: **КАНОНІЧНА БАЗА** — прямо затверджена користувачем 16.08.2026 о 18:39 +03:00.

Усі наступні структурні та функціональні зміни сайту виконувати виключно від цієї R188, доки користувач прямо не затвердить новішу канонічну повну збірку.

Детальний checkpoint:
`mavik-sklad/files/R188-MULTILANG-CANON-CHECKPOINT.md`

## АРХІТЕКТУРНИЙ КАНОН R188

R188 — **єдине багатомовне ядро**, а не окрема англійська копія сайту.

- один core;
- одна Boss-адмінка;
- одна модель даних;
- мовні пакети/конфігурація замість дублювання коду;
- стартово активні `uk` і `en`;
- архітектура розрахована на 3/4+ мов;
- українські чинні URL зберігаються;
- інші мовні версії можуть мати префікси на кшталт `/en/`;
- машинний Google Translate не є канонічною моделлю.

## BOSS / CORE

Новий core має підтримувати керування:
- статичними сторінками по мовах: створення, редагування, активація/приховування, видалення;
- пунктами меню: створення, редагування, приховування, видалення, мовні назви;
- мовами;
- зображеннями та каталогами;
- кількістю книг на головній до кнопки `Всі книги`;
- чинними книгами, блогом, анонсами, музикою, фокусами, реакціями, повідомленнями про помилки, станом сайту, deploy та робочими файлами.

Адмін може переглядати українську або іншу активну мовну версію за вибором.

## БЕЗПЕКА / DEPLOY

Багатомовна перебудова не повинна послаблювати окремий контур Boss та owner-доступу.

Живий `/_site-state/` не входить до release ZIP і не перезаписується штатним deploy. Оновлення R188 встановлюється стандартно через Boss.

## ПЕРЕВІРКА R188

Перед видачею користувачеві зафіксовано:
- PHP: 24/24 PASS;
- JavaScript: 15/15 PASS;
- EPUB: 20/20;
- биті публічні внутрішні посилання: 0;
- усі 510 файлів R187 v1 збережені;
- додано 22 core-файли;
- managed files: 532;
- Boss ZIP validator: PASS;
- smoke deploy R187 → R188: PASS;
- `/_site-state/` при deploy збережено;
- тест третьої мови: PASS.

## R187

R187 і R187 v1 більше не є поточною канонічною базою. Вони залишаються історичними попередниками R188.

## СТАРА R188 ENGLISH-ГІЛКА

Попередня модель окремого `R188 English` / `r188-english-work` більше не є робочою архітектурою сайту. Її матеріали можна використовувати як джерело вже перекладеного англійського контенту, але не як базу структури.

Англійське наповнення надалі додається всередині багатомовного R188 core.

## ПРАВИЛО ДЛЯ НАСТУПНИХ РЕЛІЗІВ

1. Кандидат не стає канонічним без прямого підтвердження користувача.
2. Нова структурна/функціональна збірка стартує від canonical R188.
3. Перед видачею обов’язково перевіряти `.mavik-release.json` валідатором Boss.
4. Не прибирати чинний контент без прямої команди користувача.
5. Мовні версії розвивати в межах одного багатомовного core, а не множенням окремих сайтів.
6. Якщо binary ZIP фізично відсутній у GitHub-складі, не стверджувати, що він там є; канонічний файл ідентифікується точним ім’ям, розміром і SHA-256.