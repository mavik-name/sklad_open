# Канон збірок MAVIK.NAME

## Поточна канонічна база

**R204 — єдина чинна структурно-функціональна CORE-база сайту.**

Затверджено користувачем: **2026-08-17**.

Усі релізи **R1–R203** є сміттям для активного структурно-функціонального контексту й не можуть використовуватися як основа для нової роботи.

Канонічний checkpoint:
`mavik-sklad/files/R204-CANON-CHECKPOINT.md`

## Файли R204

- `204 Збірка_Сайт_LISTEN-MODE-ONLY_PART-1.zip`
- `204 Збірка_Сайт_LISTEN-MODE-ONLY_PART-2.zip`

Release manifest SHA-256:
`e5c2303f6568aca817f4bf8f1fce28792fff01b5f2a5d7fe757e3d225768a4e2`

ZIP SHA-256:
- PART-1: `030f88cf1f7a2099e0a91677a9964f0e0751fc43bad4cc1dc93ec74c303fb509`
- PART-2: `321f18f4ff09e6f7bc04da1cda45407c3075807ab3100d0b3037ea482bb2d634`

Multipart set id: `r204-e5c2303f6568aca8`.

Build timestamp: `2026-08-17T17:48:18+03:00`.

## ЖОРСТКИЙ КАНОН ПІСЛЯ R204 — CODE FREEZE

Після канонізації R204 подальша звичайна робота із сайтом виконується **лише контентними PATCH/OVERLAY-пакетами БЕЗ ЗМІНИ КОДУ R204**.

Не змінювати без нового прямого рішення користувача:
- PHP;
- JavaScript;
- CSS;
- Boss-функціонал;
- шаблони/rendering architecture;
- PWA/service worker/manifest logic;
- navigation engine;
- SEO/indexing engine;
- будь-який інший CORE-код, рефакторинг чи приховану оптимізацію.

Дозволені PATCH/OVERLAY:
- книги та переклади;
- EPUB;
- обкладинки та ілюстрації;
- музика/медіа;
- блог і контентні сторінки;
- контентні дані/метадані;
- generated public/SEO artifacts, які створюються чинними механізмами R204 без зміни їхнього коду.

## Головний канон синхронізації

Контентні зміни через Boss або patch мають лишати узгодженими public state і search state, де релевантно: navigation/internal links, sitemap, robots/noindex, canonical, hreflang, structured data, IndexNow та внутрішній SEO-аудитор. Це робиться чинними можливостями R204, без модифікації коду.

## Захищена поведінка R204

- mobile: PWA install flow доступний;
- desktop: MaVik install UI не ініціалізується;
- desktop: немає audio-reader UI і немає `▶ Слухати` на book landing;
- mobile `Читати` = лише текстова читалка, без ініціалізації аудіомодуля;
- mobile `Слухати` = явний `?listen=1`, і лише тоді активується аудіочиталка;
- центральна аудіокнопка: `▶ Слухати ↔ ⏸ Пауза`;
- tap-to-start і Screen Wake Lock у listen mode збережені;
- QR донату збільшується лише в межах самого блоку, без окремого frame/modal/fullscreen;
- literal iframe/frame/frameset у збірці відсутні;
- Boss-редагування книги — inline-блок;
- порядок книг зберігається server-side і відбивається в live manifest/public rebuild;
- причина перебування книги в «Анонсах» редагується окремо для кожної книги;
- мобільне меню без нумерації;
- `/_site-state/` не перезаписується deploy/patch.

## Очищення старих релізів

За прямим рішенням користувача все до R204 видалено з активного site-release складу. У `/Сайт` лишаються тільки дві частини R204. Старі release/checkpoint-файли не є робочим контекстом. Історичні Git-коміти можуть фізично існувати в історії Git, але не мають права використовуватися як поточна база.

## Робоче правило

**Будь-яке нове наповнення стартує від R204 і доставляється тільки PATCH/OVERLAY без змін коду.**

R204 не перепаковувати та не «покращувати» як CORE без нового прямого дозволу користувача.
