# Канон збірок MAVIK.NAME

## Поточна канонічна база

**R200 — єдина чинна структурно-функціональна CORE-база сайту.**

Затверджено користувачем: **2026-08-17**.

Усі релізи **R1–R199** є застарілими як структурно-функціональні бази й не можуть використовуватися як основа для нової роботи.

Канонічний checkpoint:
`mavik-sklad/files/R200-CANON-CHECKPOINT.md`

## Файли R200

- `200 Збірка_Сайт_MOBILE-PWA_PART-1.zip`
- `200 Збірка_Сайт_MOBILE-PWA_PART-2.zip`

Release manifest SHA-256:
`bd1b6ae8229c04e3ba682f287699d1fcf8c8702098c3820e5b7be067e7a3f1fc`

ZIP SHA-256:
- PART-1: `78d2f8a920b5800b7b9c80699cb3ff35c4aafe5fcf198485b2508914d3332491`
- PART-2: `8c048f87ac6b6e7b6f5ab64fc86d525d2eddadf8759481a0a144df8e582fa890`

Build timestamp: `2026-08-17T15:18:29+03:00`.

## ЖОРСТКИЙ КАНОН ПІСЛЯ R200 — CODE FREEZE

Після канонізації R200 подальша звичайна робота із сайтом виконується **лише контентними PATCH/OVERLAY-пакетами БЕЗ ЗМІНИ КОДУ R200**.

Це означає:
- не змінювати PHP;
- не змінювати JavaScript;
- не змінювати CSS;
- не змінювати Boss-функціонал;
- не змінювати шаблони/rendering architecture;
- не змінювати PWA/service worker/manifest logic;
- не змінювати навігаційний engine;
- не змінювати SEO/indexing engine;
- не робити прихованих «покращень», рефакторингу чи оптимізації CORE під виглядом контентного patch.

Дозволені PATCH/OVERLAY:
- книги та переклади;
- EPUB;
- обкладинки та ілюстрації;
- музика/медіа;
- блог і контентні сторінки;
- контентні дані/метадані;
- згенеровані публічні індекси й SEO-артефакти, які створюються чинними механізмами без зміни їхнього коду.

**Будь-яка зміна коду заборонена без нового прямого рішення користувача, яке явно скасовує/послаблює цей freeze.** Не створювати новий CORE-реліз самостійно.

## Головний канон синхронізації

Контентні зміни через Boss або patch мають лишати узгодженими public state і search state, де релевантно: navigation/internal links, sitemap, robots/noindex, canonical, hreflang, structured data, IndexNow та внутрішній SEO-аудитор. Це робиться чинними можливостями R200, без модифікації коду.

## Захищена поведінка R200

- mobile: доступне встановлення MaVik як PWA;
- mobile: доступна аудіочиталка з однією центральною кнопкою `▶ Читати ↔ ⏸ Пауза`, tap-to-start і Screen Wake Lock;
- desktop: аудіочиталка не ініціалізується;
- desktop: MaVik install UI не ініціалізується;
- service worker і manifest лишаються технічно присутніми для мобільної PWA;
- мобільне меню без нумерації;
- `/_site-state/` не перезаписується deploy/patch.

## Робоче правило

**Будь-яке нове наповнення стартує від R200 і доставляється тільки PATCH/OVERLAY без змін коду.**

R200 не перепаковувати та не «покращувати» як CORE без нового прямого дозволу користувача.
