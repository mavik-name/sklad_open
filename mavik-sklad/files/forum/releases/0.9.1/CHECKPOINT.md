# Поточний checkpoint — 0.9.1

Повний актуальний код: source/. Пакет: forum_mavik_test_build_0.9.1.zip. База: попередній реліз ../0.9/ (історія та оригінали там збережені).

Останні прямі вказівки користувача: виправити зламану категорію зі скриншота; збільшити знак до материнського сайту; звузити весь форум до материнського сайту. Параметри взяті з локальної R300: assets/core/site.css та home HTML — 1120px, SVG 42/40px. Оригінальний mobile MaVik logo має пріоритет над взірцевим PNG. Попередній опис ширини 1480px більше не застосовувати. Нижня кава прибрана, стрілка праворуч. Реєстрація — підпис і дата у два рядки. Останній запит: делікатніша типографіка та пропорції за референсом; зменшені елементи теми, чистий фон без дубльованих кнопок.

90 нових перевірок пройдені; qa містить результати, скрипти й скриншоти. Для повторення: використати ізольовану тестову інсталяцію (попередня ../0.9/QA.md), замінити код на source/, запускати на 127.0.0.1:8099. PLAYWRIGHT_MODULE вказує пакет Playwright; браузер Edge. Тестові акаунти/БД не зберігати у GitHub.

Серверна БД не читалась і не змінювалась. Пакет не містить SQLite. Google/Telegram та весь попередній функціонал залишаються з 0.9. Повний канон/невирішені пункти: ../0.9/VISUAL-STATUS.md (нова ширина й логотип вже виправлені тут).
