# MAVIK.NAME — заплановані покращення наступного релізу

Дата фіксації: 2026-08-26
База: `R223_TTS`
Статус: **BACKLOG / NEXT RELEASE ONLY**

## Жорстке правило

Цей документ не створює нового релізу і не змінює канон. Усі майбутні зміни виконувати виключно від `R223_TTS`.

## P1 — семантика анонсів у sitemap і structured data

- `/books/kod-lehendy/` до повної публікації лишається анонсом, а не однією з опублікованих книг каталогу.
- Не подавати анонс як повноцінну опубліковану `Book`/reader-сутність у sitemap до фактичної публікації повного тексту.
- Structured data мають відображати реальний статус анонсу/майбутньої публікації.
- Анонси рахувати окремо від завершених і опублікованих творів.
- Release gate: 21 чинний опублікований твір і 21 reader не повинні змішуватися з анонсами.

## P1 — SEO-description каталогу

- Перевірити `/books/` і `/books/free/` на відсутність застарілих жорстко заданих лічильників типу `19 книг` / `19 творів`.
- Якщо кількість творів вказується в description, генерувати її з актуального набору `final` або перевіряти release-gate тестом.
- Кращий варіант для стабільності — не тримати мінливе число в SEO-description без потреби.
- Застарілий зовнішній search snippet сам по собі не вважати production-багом, якщо live HTML уже правильний; після релізу дати пошуковикам переобійти сторінки.

## P1 — обов'язковий regression gate перед наступним релізом

- 21/21 опублікованих landing-page доступні.
- 21/21 reader-page доступні й не втрачають текст/навігацію.
- TTS запускається тільки з українським системним голосом `uk-UA` / `uk-*`; іншомовний fallback не допускається.
- Інструкція зі встановлення українського голосу з'являється лише за його фактичної відсутності.
- Android та iPhone/iPad PWA/install-логіка не регресує.
- `robots.txt` дозволяє потрібних crawler-ів і закриває службові розділи.
- `sitemap.xml` проходить перевірку на дублікати, статус публікації та коректну класифікацію анонсів.
- canonical/noindex узгоджені між landing, reader та clean-text/TTS-дзеркалами.
- JSON-LD парситься без помилок і відповідає фактичному статусу сторінки.
- First-party Boss statistics зберігається без функціональної регресії.

## P2 — entity SEO MaVik

- Зберігати однозначну зв'язку `MaVik / Mavik / Мавік ↔ Віктор Макарчук` у Person/WebSite/ProfilePage structured data.
- Перевіряти актуальність `sameAs` / `rel=me` зовнішніх профілів.
- Не робити doorway/keyword-spam під DJI/Mavic; generic-запити `mavik`, `MaVik`, `мавік` просувати послідовністю entity-сигналів, зовнішніх згадок і стабільних профілів.

## P2 — crawl/index контроль після деплою

- Після наступного релізу повторити live-перевірку головної, каталогу, 21 book landing, 21 reader, `robots.txt`, `sitemap.xml`, canonical/noindex і structured data.
- При аномалії доступу порівнювати `mavik.name` з контрольними майданчиками тим самим зовнішнім механізмом, згідно з `CANON-REGISTRY.md`.
- Не робити змін DNS/хостингу/CDN лише через застарілий пошуковий кеш без підтвердженого production-багу.

## Незмінні умови

- Чинний канон до окремого прямого затвердження нового релізу: `R223_TTS`.
- Усі зміни майбутнього релізу починати від байт-ідентичного `R223_TTS.zip`.
- Старі релізи не використовувати як rollback, recovery або source-of-truth.
