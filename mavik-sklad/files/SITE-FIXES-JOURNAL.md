# MAVIK.NAME — журнал відкладених правок

Оновлено: 2026-08-17
Канонічний CORE: R207
Кандидат CORE: R208
Правило: цей журнал НЕ є дозволом змінювати CORE. Усі правки виконувати лише після окремого рішення автора; за замовчуванням R207 заморожений.

## 2026-08-17 — Desktop Boss → «Анонси»: трохи поїхала верстка картки

Статус: **ВИПРАВЛЕНО В R208 CANDIDATE / ЧЕКАЄ ПЕРЕВІРКИ НА ЖИВОМУ САЙТІ**.

Уточнення користувача: це НЕ мобільна версія. Скріншот зроблено на DESKTOP у Boss → «Анонси».

Початкове спостереження:
- на картці книги «Код легенди» правий блок керування («Редагувати», «Повернути у книги») був зміщений відносно поля причини анонсу та кнопки «Зберегти причину»;
- візуально елементи мали нерівні вертикальні відступи/позиціонування;
- функціональність не була зламана, проблема суто візуальна.

Що зроблено в R208 candidate:
- desktop-картка анонсу отримала окремий layout-клас;
- action-блок вирівняно по верхньому краю;
- форма причини анонсу має стабільну grid-структуру «поле + кнопка»;
- для вузьких екранів форма переходить в одну колонку;
- кнопки «Редагувати» / «Повернути у книги» більше не залежать від висоти поля причини.

Після встановлення R208 перевірити на живому Boss при різній ширині desktop-вікна. Якщо все стабільно — статус змінити на `ЗАКРИТО`.

Пріоритет: LOW.

## 2026-08-17 — R208 candidate: додаткові зміни за прямим дозволом автора

Статус: **ГОТОВО ДО ПЕРЕВІРКИ**.

- нова сторінка `/about/` з погодженим авторським текстом;
- Boss → `Сторінки`: редактор системних UA-сторінок `Автор`, `Приватність`, `Права`;
- прибрано hard-coded fallback `Скульптор` у книзі у фокусі; дозволено порожній стан;
- плитки головної Boss логічно згруповано;
- поштовий клієнт НЕ включено: чинна плитка поки відкриває Cityhost webmail; інтегрований IMAP/SMTP-клієнт — окреме майбутнє завдання після визначення параметрів та безпечного зберігання облікових даних.

Контрольний файл: `mavik-sklad/files/R208-CANDIDATE-CHECKPOINT.md`.
