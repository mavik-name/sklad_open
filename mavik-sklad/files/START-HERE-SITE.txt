MAVIK.NAME — START HERE
Оновлено: 18.08.2026

ПОТОЧНА КАНОНІЧНА CORE-БАЗА: R210G.
ПОТОЧНА РОБОЧА НАСТУПНА ВЕРСІЯ: R211 (WIP, НЕ КАНОН).

ВАЖЛИВО: R210G прямо канонізований автором 18.08.2026. R1–R210F не використовувати як робочу або канонічну structural/functional базу. R211 стає каноном тільки після завершення, живої перевірки та окремого прямого підтвердження автора.

Перед будь-якою роботою із сайтом:

1. Прочитати `mavik-sklad/releases/CANON.md`.
2. Прочитати `mavik-sklad/files/R210G-CANON-CHECKPOINT.md`.
3. Якщо робота стосується наступного релізу — прочитати `mavik-sklad/files/R211-WIP-CHECKPOINT.md`.
4. Не використовувати R1–R210F як active CORE context.
5. `/_site-state/` не перезаписувати штатним deploy/patch.
6. Нова гілка ChatGPT = нова розмова, але НЕ новий стан проєкту.

ПОТОЧНА ПУБЛІЧНА АРХІТЕКТУРА R210G:
- один global public shell і одна система геометрії;
- один font-family на public: `Georgia, 'Times New Roman', serif`;
- лише три breakpoint-режими: mobile `<=767`, tablet `768–1099`, desktop `>=1100`;
- shell: desktop max 1120 px, tablet max 920 px, mobile max 620 px;
- ordinary/static text card max 960 px;
- заголовок h1: 64 / 52 / 42 px;
- інформаційні сторінки `Автор`, `Приватність`, `Права`, `Платформи` та international equivalents — прості release HTML, НЕ окремий CMS/editor у Boss;
- у Boss лишаються функціональні редактори: книги, блог, музика, анонси, functional page-builder, меню, мови, медіа, SEO, пошта тощо.

R210G — ПОТОЧНІ ВІЗУАЛЬНІ ПРАВИЛА:
- `Назад` = ПЕРШИЙ пункт меню на внутрішній сторінці;
- `Назад` має compact pill-плашку в стилі `Адмінка`;
- старі `mavik-backbar-top/bottom` не монтуються і приховані;
- верхній відступ внутрішніх сторінок: desktop 61 px, tablet 53 px, mobile 44 px;
- `Автор`: вступ у власній верхній плашці, основний текст — у другій;
- homepage `У фокусі`: один card, badge `У фокусі`, обкладинка `contain`;
- mobile `Анонси`: обкладинка не ховається;
- mobile меню: збільшений читабельний шрифт;
- desktop active menu item — золотий;
- усі `Рекомендувати...` у public UI скорочені до `Поділитись`;
- `Пригостити кавою`: суми у власних плашках; desktop без зовнішнього frame/overlay; QR збільшуються тільки в межах coffee block.

МУЗИКА:
- R210G не змінює playback-код від R210F/R210E;
- `assets/app/music-library.js` і `music-data.php` збережені;
- MP3 не видаляти, не переміщати і не пакувати в точкові visual patches без окремої потреби.

ЗАХИЩЕНА ПОВЕДІНКА R210G:
- mobile-only PWA install flow `Встановити MaVik`;
- desktop: без install UI і без audio-reader UI;
- mobile `Читати` = тільки текст;
- mobile `Слухати` = `?listen=1`;
- central audio control `▶ Слухати ↔ ⏸ Пауза`;
- Boss book editor — inline;
- порядок книг server-side;
- причина анонсу редагована;
- мобільне меню без нумерації;
- rollback-safe book creation;
- `Завершена · створити EPUB` / `Бета · без EPUB`;
- EPUB temp тільки в `/_site-state/tmp/`;
- persistent custom genre registry;
- safe Boss overlay patches;
- direct URL priority over stored language preference;
- логічно згруповані Boss tiles;
- compact page-builder index;
- `Boss → Пошта` має IMAP/SMTP із PHP-IMAP або built-in TLS-IMAP fallback;
- mail credentials server-only;
- unread-mail beacon у Boss;
- порожній focus-state без hard-coded `Скульптор` fallback;
- один global maintenance mode;
- `/_site-state/` зберігається при deploy;
- English translation of `Соціальна` заборонений до нового прямого дозволу автора.

БЛОГ — ЖОРСТКИЙ КАНОН ПУБЛІКАЦІЇ:
- звичайне додавання нового блогу = **CONTENT-ONLY ZIP PATCH ЧЕРЕЗ BOSS**;
- асистент створює текст/контент і, за потреби, ілюстрацію, пакує їх у штатний Boss-compatible patch;
- користувач завантажує patch через Boss;
- blog patch не змінює PHP/JS/CSS, Boss/deployer, templates, routing, PWA, maintenance або SEO engine;
- не використовувати для штатної публікації блогу FTP, файловий менеджер, ручне копіювання в корінь, окремий `PATCH-*.php` installer або `?apply=1`;
- окремий CORE-реліз заради блогу не створювати;
- повторне застосування patch не повинно створювати дублікати й не повинно перезаписувати новіший live-content;
- ручний FTP/PHP installer — тільки аварійний recovery за окремим прямим рішенням автора.

MAINTENANCE — ЖОРСТКИЙ КАНОН:
- тільки ОДИН global maintenance для всього сайту;
- НЕ робити окремий UA maintenance;
- НЕ робити окремий EN maintenance;
- не відновлювати `.maintenance-uk-on`, `.maintenance-en-on` або locale-specific 503 logic.

ГОЛОВНИЙ КАНОН СИНХРОНІЗАЦІЇ:
Контентна зміна має лишати узгодженими public state і search state — navigation/internal links, sitemap, noindex/robots, canonical, hreflang, structured data, IndexNow та внутрішній SEO-аудитор — де релевантно й у межах чинної архітектури.

ОЧИЩЕННЯ / ІСТОРІЯ:
- R1–R210F не використовувати як active CORE context;
- R210G — чинний канон;
- R211 — поточна робоча наступна версія, не канон до прямого підтвердження автора;
- R210H як назва робочої буквеної ревізії скасована рішенням автора; її задачі перенесені в R211.

ENGLISH CONTENT
Перед English-роботою ОБОВ’ЯЗКОВО прочитати:
- `mavik-sklad/files/EN-TRANSLATION-START-HERE.md`;
- `mavik-sklad/translations/en/SITE-PUBLISHED-SOURCE-HARD-CANON.md`;
- `mavik-sklad/files/EN-SOURCE-POLICY.md`;
- `mavik-sklad/translations/en/CONTINUATION-CANON.md`;
- `mavik-sklad/translations/en/STATUS.md`.

Єдине джерело нового English-тексту книги — канонічний український текст, фактично опублікований на mavik.name. Якщо exact site-source недоступний — переклад зупинити, не домислювати.

Для `Соціальна` діє STOP-канон `mavik-sklad/files/SOCIALNA-NO-ENGLISH-TRANSLATION-CANON.md`: не перекладати англійською до нового прямого дозволу автора.
