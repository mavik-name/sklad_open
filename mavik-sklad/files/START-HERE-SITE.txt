MAVIK.NAME — START HERE
Оновлено: 17.08.2026

ПОТОЧНА КАНОНІЧНА CORE-БАЗА: R210.

Перед будь-якою роботою із сайтом:

1. Прочитати `mavik-sklad/releases/CANON.md`.
2. Прочитати `mavik-sklad/files/R210-CANON-CHECKPOINT.md`.
3. Не використовувати R1–R209 як робочу або канонічну structural/functional базу.
4. ГОЛОВНЕ ПРАВИЛО ПІСЛЯ R210: працювати тільки контентними PATCH/OVERLAY-пакетами БЕЗ ЗМІНИ CORE-КОДУ.
5. НЕ змінювати PHP/JS/CSS, Boss/deployer, templates, PWA/service worker/manifest logic, navigation engine, SEO/indexing engine, maintenance architecture, locale routing або інший CORE-код R210.
6. НЕ робити refactoring, optimization чи «покращення» ядра самостійно.
7. Дозволені patch-и: книги/переклади, EPUB, обкладинки, ілюстрації, музика, блог/контентні сторінки, контентні дані/метадані та generated public/SEO artifacts, якщо вони створюються чинними механізмами R210 без зміни CORE.
8. Будь-яка зміна ядра можлива лише після нового прямого рішення користувача.
9. ГОЛОВНИЙ КАНОН СИНХРОНІЗАЦІЇ: контентна зміна має лишати узгодженими public state і search state — navigation/internal links, sitemap, noindex/robots, canonical, hreflang, structured data, IndexNow та внутрішній SEO-аудитор — де релевантно й у межах чинних можливостей R210.
10. `/_site-state/` не перезаписувати штатним deploy/patch.
11. Нова гілка ChatGPT = нова розмова, але НЕ новий стан проєкту.

R210 protected behavior:
- mobile: PWA install flow `Встановити MaVik`;
- desktop: MaVik install UI не ініціалізується;
- desktop: немає audio-reader UI і немає `▶ Слухати` на сторінках книг;
- mobile `Читати` = тільки текст, аудіомодуль не ініціалізується;
- mobile `Слухати` = `?listen=1`, і тільки тоді запускається аудіочиталка;
- central audio control `▶ Слухати ↔ ⏸ Пауза`;
- tap-to-start і Screen Wake Lock у listen mode;
- QR донату збільшується лише до ширини свого блоку;
- Boss book editor — inline;
- порядок книг server-side;
- причина анонсу редагована;
- мобільне меню без нумерації;
- rollback-safe book creation;
- `Завершена · створити EPUB` / `Бета · без EPUB`;
- EPUB temp тільки в `/_site-state/tmp/`;
- persistent custom genre registry;
- safe Boss overlay patches;
- direct URL priority over stored language preference;
- логічно згруповані Boss tiles;
- `Boss → Сторінки` має drag&drop-конструктор ключових системних сторінок з preview і відновленням попередньої версії;
- compact page-builder index без обрізання назв;
- `Boss → Пошта` має IMAP/SMTP-клієнт із PHP-IMAP або built-in TLS-IMAP fallback;
- mail credentials server-only, не входять у release/state export;
- порожній focus-state без hard-coded `Скульптор` fallback;
- `/about/` містить погоджений авторський текст і редагується через Boss.

MAINTENANCE — ЖОРСТКИЙ КАНОН:
- тільки ОДИН global maintenance для всього сайту;
- НЕ робити окремий UA maintenance;
- НЕ робити окремий EN maintenance;
- не відновлювати `.maintenance-uk-on`, `.maintenance-en-on` або `maintenance/index.php` як locale-specific 503 logic.

ОЧИЩЕННЯ / ІСТОРІЯ:
- R1–R209 не використовувати як active CORE context;
- R207 — попередній канон;
- R208/R209 — неканонічні кандидати, поглинуті R210;
- єдина чинна робоча база — R210.

ENGLISH CONTENT
Перед English-роботою ОБОВ’ЯЗКОВО прочитати:
- `mavik-sklad/files/EN-TRANSLATION-START-HERE.md`;
- `mavik-sklad/translations/en/SITE-PUBLISHED-SOURCE-HARD-CANON.md`;
- `mavik-sklad/files/EN-SOURCE-POLICY.md`;
- `mavik-sklad/translations/en/CONTINUATION-CANON.md`;
- `mavik-sklad/translations/en/STATUS.md`.

English-контент доставляти через штатні overlay-патчі R210, без зміни locale routing і без окремого maintenance для `/en/`.

Єдине джерело нового English-тексту книги — канонічний український текст, фактично опублікований на mavik.name. Якщо exact site-source недоступний — переклад зупинити, не домислювати.

Для `Соціальна` діє STOP-канон `mavik-sklad/files/SOCIALNA-NO-ENGLISH-TRANSLATION-CANON.md`: не перекладати англійською до нового прямого дозволу автора.

R210 — єдиний чинний site CORE canon. Після R210 — PATCH/OVERLAY ONLY, NO CORE CODE CHANGES.
