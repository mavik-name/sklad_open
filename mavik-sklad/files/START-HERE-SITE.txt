MAVIK.NAME — START HERE

ЄДИНИЙ КАНОН: R230_Blog

- `R230_Blog` — єдина канонічна, робоча, recovery і reference база сайту.
- Канон не змінювати без прямої команди автора.
- `R230` зберігається як verified rollback reserve.
- `R229` зберігається як старіша verified rollback reserve.
- Чинний сайт і робочий контекст — українські.

CANON IDENTITY
- file: `R230_Blog.zip`
- size: `28005701` bytes
- SHA-256: `ad1d46c7209e53e0c4058ad033a30946a3cffc85b5ec9677f8af4d2d7bdcc348`
- checkpoint: `mavik-sklad/files/R230_BLOG-CANON.md`

CURRENT RELEASE CANDIDATE: R231_LinkFix
- base: `R231`;
- full file: `R231_LinkFix.zip`;
- full size: `28007089` bytes;
- full SHA-256: `d0ec1a1c974be773e1446d7dc128b462e3bcc3c16c2ca166020c7d5752961ec2`;
- status: VALIDATED / NOT CANONICAL;
- checkpoint: `mavik-sklad/files/R231-LINKFIX-CHECKPOINT.md`.

SAFE LIVE FIX IF R231 IS ALREADY INSTALLED
- `R231_LinkFix_PATCH.zip`;
- size: `81983` bytes;
- SHA-256: `5d4ae0ed53a666a0b1fdf0de45e00695b906e4f8da426a9a54f255deca5bf080`;
- changes exactly `boss/index.php`;
- does NOT overwrite blog posts, books, user media, EPUB or `/_site-state/`.

R231 / R231_LinkFix INCLUDES
- весь mail/core функціонал R230/R230_Blog;
- BookFix: genre checkbox filters optional; fallback from `Жанр / підзаголовок`;
- блог-редактор `Візуальний` / `</> HTML`;
- внутрішній scroll редактора;
- LIVE CONTENT FIRST blog snapshot/restore in deploy;
- LinkFix: blog links no longer depend on deprecated `document.execCommand('createLink')`;
- selected text is linked by DOM Range and real `<a href>`;
- internal `До допису…` picker uses the same mechanism;
- visual HTML, HTML-code textarea and hidden `body_html` synchronize immediately after link insertion.

INSTALLATION
A) If R231 is already installed: install only `R231_LinkFix_PATCH.zip`.
B) If production is still R230_Blog: install R231 bootstrap protection first, then the full `R231_LinkFix.zip`.

VALIDATION R231_LinkFix
- PHP 31/31 PASS;
- JS 15/15 PASS;
- inline blog editor JS PASS;
- deprecated createLink removed PASS;
- DOM Range insertion present PASS;
- immediate body_html sync PASS;
- JSON 12/12 PASS;
- EPUB 21/21 PASS;
- manifest 268/268 PASS;
- full ZIP integrity PASS;
- patch ZIP integrity PASS;
- patch simulation against exact R231: only `boss/index.php` differs.

LIVE CONTENT FIRST — НЕПОРУШНЕ ПРАВИЛО
- release ZIP = база коду/структури, а не автоматично найсвіжіший production content;
- full deploy не має права перезаписувати новіший Boss-managed live content;
- при конфлікті старішого ZIP із новішим live content зберігати live-версію, доки автор прямо не наказав інакше.

ROLLBACK RESERVES
- `mavik-sklad/files/R230-RESERVE.md`
- `mavik-sklad/files/R229-RESERVE.md`

Для production-аудиту використовувати чинний live-сайт + канонічну базу; search cache/snippets не видавати за live-стан.
