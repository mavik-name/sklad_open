MAVIK.NAME — START HERE
Оновлено: 18.08.2026

ПОТОЧНА КАНОНІЧНА CORE-БАЗА: R210.
АКТИВНИЙ КАНДИДАТ ДЛЯ ЖИВОЇ ПЕРЕВІРКИ: R210G.

ВАЖЛИВО: R210G — буквена кандидатна ревізія R210, НЕ канон і НЕ R211. Канон змінюється тільки після прямого підтвердження автора.

Перед будь-якою роботою із сайтом:

1. Прочитати `mavik-sklad/releases/CANON.md`.
2. Прочитати `mavik-sklad/files/R210-CANON-CHECKPOINT.md`.
3. Для поточного стану ОБОВ’ЯЗКОВО прочитати `mavik-sklad/files/R210G-CANDIDATE-CHECKPOINT.md`.
4. Не використовувати R1–R209 як робочу або канонічну structural/functional базу.
5. R210 лишається каноном, доки автор після живої перевірки прямо не затвердить новішу буквенную ревізію.
6. `/_site-state/` не перезаписувати штатним deploy/patch.
7. Нова гілка ChatGPT = нова розмова, але НЕ новий стан проєкту.
8. Якщо live встановлено R210F — R210G можна накласти FTP-патчем `210G ПАТЧ_FTP_поверх_R210F.zip`; для чистого розгортання використовувати R210G FTP-FULL.

ПОТОЧНА ПУБЛІЧНА АРХІТЕКТУРА R210E/F/G:
- один global public shell і одна система геометрії;
- один font-family на public: `Georgia, 'Times New Roman', serif`;
- лише три breakpoint-режими: mobile `<=767`, tablet `768–1099`, desktop `>=1100`;
- shell: desktop max 1120 px, tablet max 920 px, mobile max 620 px;
- ordinary/static text card max 960 px;
- заголовок h1: 64 / 52 / 42 px;
- інформаційні сторінки `Автор`, `Приватність`, `Права`, `Платформи` та international equivalents — прості release HTML, НЕ окремий CMS/editor у Boss;
- майбутні зміни таких static pages робити малими ZIP overlay/patch, якщо не змінюється CORE-логіка;
- у Boss лишаються функціональні редактори: книги, блог, музика, анонси, functional page-builder, меню, мови, медіа, SEO, пошта тощо.

R210G — ПОТОЧНІ ВІЗУАЛЬНІ ПРАВИЛА:
- `Назад` більше не стоїть окремо над/під контентом;
- `Назад` = ПЕРШИЙ пункт меню на внутрішній сторінці;
- `Назад` має compact pill-плашку в стилі `Адмінка`;
- старі `mavik-backbar-top/bottom` не монтуються і приховані;
- після перенесення `Назад` верхній відступ внутрішніх сторінок зменшено на 50%: desktop 61 px, tablet 53 px, mobile 44 px;
- `Автор`: вступ `MaVik / Віктор Макарчук · MaVik / Письменництво...` у власній верхній плашці, основний текст — у другій; у вступній плашці є додаткове повітря зверху;
- homepage `У фокусі`: один card, badge `У фокусі` на card, обкладинка видима ПОВНІСТЮ (`contain`), без плаваючої чорної картки, що перекриває CTA;
- mobile `Анонси`: обкладинка не ховається;
- mobile меню: збільшений читабельний шрифт;
- desktop active menu item — золотий;
- усі `Рекомендувати...` у public UI скорочені до `Поділитись`;
- `Пригостити кавою`: суми у власних плашках на desktop і mobile;
- desktop coffee: БЕЗ зовнішнього frame/overlay — одна центральна плашка на 100dvh, border/radius/shadow зовні відсутні;
- QR збільшуються тільки в межах свого coffee block, без окремого fullscreen.

МУЗИКА:
- після короткого live-збою відтворення самостійно відновилося; це вважається ймовірною серверною/мережевою проблемою, а не підтвердженим багом плеєра;
- R210G НЕ змінює playback-код;
- `assets/app/music-library.js` і `music-data.php` у R210G байт-у-байт однакові з R210F/R210E;
- MP3 не видаляти, не переміщати і не пакувати в точкові visual patches без окремої потреби.

R210 protected behavior, яке всі буквені ревізії мають повністю успадковувати:
- mobile-only PWA install flow `Встановити MaVik`;
- desktop: MaVik install UI не ініціалізується;
- desktop: немає audio-reader UI і немає `▶ Слухати` на сторінках книг;
- mobile `Читати` = тільки текст, аудіомодуль не ініціалізується;
- mobile `Слухати` = `?listen=1`, і тільки тоді запускається аудіочиталка;
- central audio control `▶ Слухати ↔ ⏸ Пауза`;
- tap-to-start і Screen Wake Lock у listen mode;
- Boss book editor — inline;
- порядок книг server-side;
- причина анонсу редагована;
- мобільне меню без нумерації;
- rollback-safe book creation;
- `Завершена · створити EPUB` / `Бета · без EPUB`;
- EPUB temp тільки в `/_site-state/tmp/`;
- persistent custom genre registry;
- safe Boss overlay patches;
- direct URL priority over stored language preference;
- логічно згруповані Boss tiles;
- compact page-builder index без обрізання назв;
- `Boss → Пошта` має IMAP/SMTP-клієнт із PHP-IMAP або built-in TLS-IMAP fallback;
- mail credentials server-only, не входять у release/state export;
- unread-mail beacon у Boss: load + 60s + visibility return; 0 прихований;
- порожній focus-state без hard-coded `Скульптор` fallback;
- один global maintenance mode;
- `/_site-state/` зберігається при deploy;
- English translation of `Соціальна` ЗАБОРОНЕНИЙ до нового прямого дозволу автора.

MAINTENANCE — ЖОРСТКИЙ КАНОН:
- тільки ОДИН global maintenance для всього сайту;
- НЕ робити окремий UA maintenance;
- НЕ робити окремий EN maintenance;
- не відновлювати `.maintenance-uk-on`, `.maintenance-en-on` або locale-specific 503 logic.

ГОЛОВНИЙ КАНОН СИНХРОНІЗАЦІЇ:
Контентна зміна має лишати узгодженими public state і search state — navigation/internal links, sitemap, noindex/robots, canonical, hreflang, structured data, IndexNow та внутрішній SEO-аудитор — де релевантно й у межах чинних можливостей канонічної/затвердженої архітектури.

ОЧИЩЕННЯ / ІСТОРІЯ:
- R1–R209 не використовувати як active CORE context;
- R210 — чинний канон;
- R210A–R210F — попередні буквені кандидати/ітерації, не канон;
- R210E був live-встановлений як clean global candidate і став базою для F/G visual corrections;
- R210G — поточний кандидат для живої перевірки;
- R210 binary видаляти з active `/Сайт/` лише ПІСЛЯ прямої канонізації новішої збірки.

ENGLISH CONTENT
Перед English-роботою ОБОВ’ЯЗКОВО прочитати:
- `mavik-sklad/files/EN-TRANSLATION-START-HERE.md`;
- `mavik-sklad/translations/en/SITE-PUBLISHED-SOURCE-HARD-CANON.md`;
- `mavik-sklad/files/EN-SOURCE-POLICY.md`;
- `mavik-sklad/translations/en/CONTINUATION-CANON.md`;
- `mavik-sklad/translations/en/STATUS.md`.

Єдине джерело нового English-тексту книги — канонічний український текст, фактично опублікований на mavik.name. Якщо exact site-source недоступний — переклад зупинити, не домислювати.

Для `Соціальна` діє STOP-канон `mavik-sklad/files/SOCIALNA-NO-ENGLISH-TRANSLATION-CANON.md`: не перекладати англійською до нового прямого дозволу автора.
