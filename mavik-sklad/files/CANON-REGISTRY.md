# CANON REGISTRY — MAVIK.NAME

## ЄДИНИЙ КАНОН
`R225_OK`

Статус: **AUTHOR APPROVED / OFFICIAL / CANONICAL / ONLY WORKING BASE**
Дата: 2026-08-28

Identity:
- file: `R225_OK.zip`
- size: `27830938` bytes
- SHA-256: `daba3a1f47ef431aa54dc9376cf7262def75de2681ae472aad0818f96b5a4e16`

Правила:
- усі зміни, виправлення, recovery та наступні релізи починати тільки від `R225_OK`;
- усі релізи, кандидати, патчі, rollback-копії, release-аудити, release-маркери та старі site-control документи до `R225_OK` фізично вилучаються з release-сховищ і не мають жодного авторитету;
- не існує попереднього rollback reserve: відновлення старої збірки можливе лише після нового прямого наказу автора;
- чинний сайт і робочий контекст — українські;
- актуальний стан production визначається `R225_OK` + свіжими live-даними. Search snippets/cache відокремлювати від live-стану.

## ПОСТІЙНЕ ПРАВИЛО АУДИТУ
- Для досліджень `mavik.name` використовувати тільки робочий веб-механізм, який реально має зовнішній доступ і читає production.
- Не використовувати ізольований контейнерний DNS/`curl` або інший канал без підтвердженого зовнішнього доступу як джерело висновків про DNS, HTTP/TLS, crawlability чи індексацію.
- Якщо під час перевірки `mavik.name` виникає аномалія доступу, crawl або пошукової видимості, порівняти тим самим механізмом із `booknet.ua`, `arkush.net` і `novelklo.com`.

Не відновлювати артефакти до `R225_OK` без нового прямого наказу автора.
