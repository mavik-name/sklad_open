# CANON REGISTRY — MAVIK.NAME

## ЄДИНИЙ КАНОН
`R223_TTS`

Статус: **AUTHOR APPROVED / CANONICAL / ONLY WORKING BASE**

Правила:
- усі зміни, виправлення, recovery та наступні релізи починати тільки від `R223_TTS`;
- усі релізи, збірки, rollback-копії, release-аудити, release-маркери та старі site-control документи до `R223_TTS` — сміття/історія і не мають жодного авторитету;
- R222, R222_Statistic, R217, R216, R215 та всі старші релізи не є резервами;
- чинний сайт і робочий контекст — українські;
- актуальний стан production визначається тільки `R223_TTS` + свіжими live-даними + актуальним Search Console.

## ПОСТІЙНЕ ПРАВИЛО АУДИТУ
- Для досліджень `mavik.name` використовувати тільки робочий веб-механізм, який реально має зовнішній доступ і читає production.
- Не використовувати ізольований контейнерний DNS/`curl` або інший канал без підтвердженого зовнішнього доступу як джерело висновків про DNS, HTTP/TLS, crawlability чи індексацію.
- Якщо під час перевірки `mavik.name` виникає будь-яка аномалія доступу, crawl або пошукової видимості, виконати контрольне порівняння з `booknet.ua`, `arkush.net` і `novelklo.com` тим самим робочим механізмом і за тими самими параметрами.
- Порівнювати щонайменше: доступність домену/сторінки, відкриття ключових URL, пошукову видимість внутрішніх сторінок, свіжість crawl та поведінку crawler/index pipeline.
- Проблему `mavik.name` вважати специфічною для сайту лише якщо контрольні майданчики за тим самим тестом працюють нормально.

Не відновлювати старі релізні артефакти без нового прямого наказу автора.
